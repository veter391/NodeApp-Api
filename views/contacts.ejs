<!DOCTYPE html>
<html lang="en">
    
<%- include('./partials/head.ejs') %>

<body>
    <header>
        <%- include('./partials/nav.ejs') %>
    </header>
    <h1>Contacts</h1>
    <ol>
        <% if (contacts.length) { %>
            <% contacts.forEach(({id, name, link})=> { %>
                <li><a href="#"><%= name %></a></li>
                <% }) %>
            <% } %>
    </ol>
</body>
</html>